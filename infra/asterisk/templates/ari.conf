[general]
enabled = yes
pretty = yes

; Credentials worden bij container-start ingevuld door entrypoint.sh (envsubst) uit
; ARI_PASSWORD (infra/.env). Geen secrets in git.
[cc-backend]
type = user
read_only = no
password = ${ARI_PASSWORD}
