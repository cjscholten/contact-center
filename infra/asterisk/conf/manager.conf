; AMI: gebruikt door de backend voor wachtrijleden (QueueAdd/QueueRemove/QueuePause);
; dat kan niet via ARI. Alleen bereikbaar maken vanaf backend/beheer-IP's (NSG).
[general]
enabled = yes
bindaddr = 0.0.0.0
port = 5038

[cc-backend]
secret = changeme-dev
read = agent
write = agent
