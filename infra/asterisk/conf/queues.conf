; app_queue wordt niet meer gebruikt: de backend bestuurt de wachtrijen server-side
; via ARI (holding-brug met wachtmuziek per wachtrij, mixing-brug per gesprek, en
; agent-dispatch in de backend). Bewust leeg gelaten zodat geen enkele app_queue-
; wachtrij actief is.

[general]
