[general]
persistentmembers = no

[support]
strategy = rrmemory
timeout = 20
retry = 2
; nawerktijd regelen we later vanuit de backend (pause/unpause), niet hier
wrapuptime = 0
musicclass = default
joinempty = yes
leavewhenempty = no
announce-position = yes
announce-frequency = 30
announce-holdtime = no
; POC: statisch lid; later voegt de backend agents dynamisch toe op basis van login
member => PJSIP/agent1001
