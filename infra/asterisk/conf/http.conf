; Ingebouwde HTTP-server: gebruikt voor ARI (REST + events) en
; het WebSocket-endpoint /ws voor de browser-agents.
; Alleen bereikbaar maken vanaf het beheer-/agentnetwerk, niet vanaf internet.
[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
