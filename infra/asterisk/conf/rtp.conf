[general]
; Kleine range voor de POC; vergroot bij meer gelijktijdige gesprekken
; (2 poorten per mediastroom aanhouden) en open deze range in de firewall.
rtpstart = 10000
rtpend = 10100

; Draait Asterisk in Azure (of achter andere 1:1 NAT) en verbinden agents
; via internet? Vul dan de mapping privé-IP => publiek IP van de VM in,
; anders biedt ICE alleen het (onbereikbare) privé-IP aan de browser aan.
; De trunk-leg naar de SBC blijft gewoon over het privé-IP lopen.
;[ice_host_candidates]
;10.0.1.4 => 20.0.0.0
